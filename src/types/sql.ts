export interface SqlNullableString {
  String: string;
  Valid: boolean;
}

export interface SqlNullableNumber {
  Int64: number;
  Valid: boolean;
}

export interface SqlNullableBool {
  Bool: boolean;
  Valid: boolean;
}

export interface SqlNullableTime {
  Time: string;
  Valid: boolean;
}
